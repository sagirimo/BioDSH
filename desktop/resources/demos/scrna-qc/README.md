# 单细胞数据一键体检

**数据**：`data/pbmc3k_raw.h5ad`（人外周血单个核细胞，10x 原始计数）。

一句话交给智能体，它自动统计细胞数/基因数、线粒体比例分布，判断质量、给出过滤阈值，并画一张质控概览图（都在 `outputs/`）。适合刚拿到单细胞数据、想先"体检"一下的人。

**照着做**：把你自己的 `.h5ad` 放进「分析」页工作区，用同样的问法提问即可。
